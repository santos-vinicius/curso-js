const nome = 'Vinicius';
const sobrenome = 'Santos';
const idade = '23';
const cpf = 49096563832;

function soma(x, y) {
  return x + y;
}

export default (x, y) => x * y;

export { nome, sobrenome, idade, cpf, soma };
