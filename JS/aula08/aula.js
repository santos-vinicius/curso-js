// Tipos primitivos de dados em JavaScript
// String
const nome = 'Vinicius';
const nome1 = 'Vinicius';
const nome2 = `Vinicius`;

// Number
const num1 = 10;
const num2 = 10.52;

// Undefined
let nomeAluno; // Não aponta para local nenhum na memória.

// Null
let sobrenomeAluno; // Não aponta para local nenhum da memória.

// Boolean = true, false (lógico)
const aprovado = false;
